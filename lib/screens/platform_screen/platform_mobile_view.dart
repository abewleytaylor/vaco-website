import 'package:flutter/material.dart';

class PlatformMobileView extends StatelessWidget {
  const PlatformMobileView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Coming soon: Platform"),
    );
  }
}
